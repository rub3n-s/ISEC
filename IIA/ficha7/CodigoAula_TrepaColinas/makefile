CC = gcc
SOURCE = $(wildcard *.c)
EXECUTABLE = exec

all: 
	@ $(CC) $(SOURCE) -o $(EXECUTABLE)
	@ echo "Compilation finished sucessfully."

clean:
	@ rm $(EXECUTABLE).exe -f
	@ echo "Directory cleaned."